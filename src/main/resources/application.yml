
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pyszne_pl
    username: postgres
    password: 14099041MaR!
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update #Update DB based on entities